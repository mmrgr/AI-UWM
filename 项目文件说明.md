# WaterMet² 项目文件说明

项目已经按“程序、前端、示例、数据、文档、测试、运行产物”分类。为避免破坏论文复现路径，没有随意移动既有文件。

## 最常用入口

| 文件 | 用途 |
|---|---|
| `启动WaterMet2 Studio.cmd` | Windows 双击启动前端和模型服务 |
| `README.md` | 模型安装、配置和命令行完整说明 |
| `frontend/README.md` | 只有数据的用户从 CSV 到结果的逐步教程 |
| `FUNCTIONAL_COVERAGE_CN.md` | 与论文和官方手册的功能对照 |
| `REPRODUCTION_REPORT_CN.md` | 论文精读、Oslo 案例和复现边界 |

## 目录结构

```text
watermet2/
├─ 启动WaterMet2 Studio.cmd   # 推荐入口
├─ src/watermet2_repro/       # Python 模型、校验、API 和启动器
├─ frontend/                  # React/TypeScript 前端
│  ├─ src/                    # 前端源代码
│  ├─ dist/                   # 构建后的可直接服务网页（自动生成）
│  └─ README.md               # 新手教程
├─ examples/demo_full/        # 全功能项目、时间序列和分析配置示例
├─ data/                      # WaterMet² 参数数据库和 Oslo 参数
├─ scripts/                   # 论文复现、模拟数据和全流程验证脚本
├─ tests/                     # Python 自动测试
├─ references/                # 来源元数据及本地参考资料
├─ validation_artifacts/      # 全流程验证报告
├─ artifacts/                 # 论文复现图表和结果
├─ output/                    # 用户运行输出
└─ tmp/                       # 临时文件，可安全删除
```

## 启动程序做了什么

双击 `启动WaterMet2 Studio.cmd` 后：

1. 检查 `frontend/node_modules`，首次运行时自动执行 `npm install`；
2. 检查前端源代码是否比 `frontend/dist` 新，必要时自动构建；
3. 检查 FastAPI/uvicorn，缺少时自动安装项目的 `studio` 依赖；
4. 用一个 Python 进程同时提供 `/api/*` 和前端静态页面；
5. 自动打开 `http://127.0.0.1:8000`。

因此日常使用不再需要分别启动 5173 和 8000 两个服务。

## 哪些文件可以清理

以下内容都是自动生成的：

- `.pytest_cache/`
- `tmp/`
- `frontend/dist/`
- `frontend/coverage/`
- Python `__pycache__/`

`frontend/node_modules/` 也可以删除，但下一次启动会重新下载依赖。

不要随意删除：

- `examples/`：完整功能示例和测试基准；
- `data/`：模型数据库与论文参数；
- `references/`：来源和复现依据；
- `validation_artifacts/VALIDATION_REPORT_CN.md`；
- 自己放在 `output/` 中的分析结果。
